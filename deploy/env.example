# Скопируйте в корень репозитория: cp deploy/env.example .env
#
# Опционально. В prod API идёт на тот же origin, что и страница (IP или домен).
# Нужен только для нестандартных схем (отдельный хост API).
# VITE_API_BASE_URL=http://lk.ruc.su

# Порт на хосте
HTTP_PORT=80
