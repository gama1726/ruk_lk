# Скопируйте в корень репозитория: cp deploy/env.example .env
#
# Публичный URL сайта (без слэша). Должен совпадать с тем, как открываете ЛК в браузере.
VITE_API_BASE_URL=http://203.0.113.10

# Порт на хосте
HTTP_PORT=80
