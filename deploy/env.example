# Скопируйте в корень репозитория: cp deploy/env.example .env
#
# Prod: тот же origin, что в браузере (обычно можно не задавать — фронт берёт window.location.origin).
# Если задаёте явно:
# VITE_API_BASE_URL=https://my.ruc.su

# Порт Docker web на хосте (внешний nginx проксирует сюда)
HTTP_PORT=80
