# Профиль prod: API только на localhost, за nginx.
server.address=127.0.0.1
server.port=8080
server.forward-headers-strategy=framework

# После настройки HTTPS в nginx раскомментируйте:
# server.servlet.session.cookie.secure=true
